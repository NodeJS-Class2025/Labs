<!DOCTYPE html>
<html lang="uk">
	<head>
		<meta charset="UTF-8" />
		<title>Теми форуму</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
	</head>
	<body>
		<%- include ('header') %>
		<h1>Теми форуму</h1>

		<% if (topics.length === 0) { %>
		<p>Тем поки немає.</p>
		<% } else { %>
		<ul>
			<% topics.forEach(topic => { %>
			<li>
				<a href="/topics/<%= topic.id %>"><%= topic.title %></a><br />
				Автор: #<%= topic.userId %><br />
				Дата створення: <%= `${topic.createdAt.toISOString().split('T')[0]}
				${topic.createdAt.toISOString().split('T')[1].split('.')[0]}` %>
			</li>
			<% }) %>
		</ul>
		<% } %>

		<a href="/topics/create-topic" class="btn btn-primary"
			>Створити нову тему</a
		>
	</body>
</html>
