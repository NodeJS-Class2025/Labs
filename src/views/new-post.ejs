<!DOCTYPE html>
<html lang="uk">
	<head>
		<meta charset="UTF-8" />
		<title>Новий допис</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
	</head>
	<body>
		<%- include('header') %>
		<div class="container">
			<h1>Новий допис для теми #<%= topicId %></h1>
			<form action="/posts/topic/<%= topicId %>" method="POST">
				<div class="mb-3">
					<label for="description" class="form-label">Опис</label>
					<textarea
						name="description"
						id="description"
						class="form-control"
						rows="4"
						required
					></textarea>
				</div>
				<button type="submit" class="btn btn-success">
					Опублікувати
				</button>
			</form>
			<a href="/topics/<%= topicId %>">← Назад до теми</a>
		</div>
	</body>
</html>
