<!DOCTYPE html>
<html lang="uk">
	<head>
		<meta charset="UTF-8" />
		<title><%= topicTitle %></title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
	</head>
	<body>
		<%- include ('header') %>
		<h1>Тема #<%= topicId %>: <%= topicTitle %></h1>

		<% if (posts.length === 0) { %>
		<p>Ще немає дописів.</p>
		<% } else { %>
		<ul>
			<% posts.forEach(post => { %>
			<li>
				<p><%= post.description %></p>
				<small
					>Автор: #<%= post.userId %>, дата: <%=
					`${post.createdAt.split('T')[0]}
					${post.createdAt.split('T')[1].split('.')[0]}` %></small
				>
			</li>
			<% }) %>
		</ul>
		<% } %>

		<a href="/topics/<%= topicId %>/create-post" class="btn btn-primary"
			>Додати новий допис</a
		>
		<br /><br />

		<a href="/topics">← Назад до тем</a>
	</body>
</html>
